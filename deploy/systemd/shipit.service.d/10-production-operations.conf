# Production operations: per-release environment file.
# Restart, StartLimit, OnFailure, ENVIRONMENT=production, and hardening
# are now in the main shipit.service unit file.
#
# EnvironmentFile is kept here because the path is release-layout-dependent
# and set by deploy-production.sh.

[Service]
EnvironmentFile=-/opt/shipit/.release-env
