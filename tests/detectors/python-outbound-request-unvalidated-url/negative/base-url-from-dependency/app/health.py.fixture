from fastapi import APIRouter, Depends

import httpx

router = APIRouter()


@router.get("/health")
async def health(base_url: str = Depends(get_base_url)):
    return httpx.get(f"{base_url}/health").json()
