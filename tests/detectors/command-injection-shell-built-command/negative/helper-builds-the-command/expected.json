{
  "description": "The command is assembled by a call the rule does not follow into, so the trace stops and the finding's text says the trace is local. The mutation inlines the concatenation, which must fire.",
  "expect": [],
  "forbid": [
    "command-injection-shell-built-command"
  ]
}
