from fastapi import APIRouter, Depends
import subprocess

router = APIRouter()


def get_command() -> str:
    return "ls -la"


@router.get("/run")
async def run(cmd: str = Depends(get_command)):
    subprocess.run(cmd, shell=True)
