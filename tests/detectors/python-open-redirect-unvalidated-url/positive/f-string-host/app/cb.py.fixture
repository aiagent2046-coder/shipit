from fastapi import APIRouter
from starlette.responses import RedirectResponse

router = APIRouter()


@router.get("/cb")
async def cb(host: str):
    return RedirectResponse(f"https://{host}/callback")
