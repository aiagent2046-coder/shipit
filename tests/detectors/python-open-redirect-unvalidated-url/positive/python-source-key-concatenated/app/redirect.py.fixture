from fastapi import APIRouter, Request
from starlette.responses import RedirectResponse

router = APIRouter()


@router.get("/go")
async def go(request: Request):
    return RedirectResponse(request.query_params.get("ne" + "xt"))
