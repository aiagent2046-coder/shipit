from fastapi import APIRouter, Response

router = APIRouter()


@router.post("/authenticate")
async def authenticate(response: Response, session_token: str):
    response.set_cookie(key="user_auth", value=session_token, secure=True)
    return {"ok": True}
