from fastapi import APIRouter, Response

router = APIRouter()


@router.post("/login")
async def login(response: Response, token: str):
    mode = "None"
    response.set_cookie("session_id", token, httponly=True, samesite=mode)
    return {"ok": True}
