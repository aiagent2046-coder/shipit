from fastapi import APIRouter, Response

router = APIRouter()


@router.post("/login")
async def login(response: Response, token: str):
    response.set_cookie("session_" + "id", token, samesite="none")
    return {"ok": True}
