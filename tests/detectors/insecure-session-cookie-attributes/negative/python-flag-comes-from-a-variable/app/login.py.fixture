from fastapi import APIRouter, Response

HTTP_ONLY = True
router = APIRouter()


@router.post("/login")
async def login(response: Response, token: str):
    response.set_cookie("session_id", token, httponly=HTTP_ONLY)
    return {"ok": True}
