{
  "description": "The flag is bound in a SIBLING function. Python never runs that binding where the call is, so it is not evidence -- the same claim binding-in-other-scope pins for archive receivers, now pinned for values. A reader that followed it would assert what the file cannot show. The mutation writes the literal False at the call, which must fire.",
  "expect": [],
  "forbid": [
    "insecure-session-cookie-attributes"
  ]
}
