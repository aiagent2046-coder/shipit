from fastapi import APIRouter, Response

router = APIRouter()


@router.post("/login")
async def login(response: Response, token: str):
    response.set_cookie("session_id", token, httponly=flag)
    return {"ok": True}


def defaults():
    flag = False
