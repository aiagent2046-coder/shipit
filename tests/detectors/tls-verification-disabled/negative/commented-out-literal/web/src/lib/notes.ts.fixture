// rejectUnauthorized: false was removed in v2 -- do not re-add it
// process.env.NODE_TLS_REJECT_UNAUTHORIZED setup lived here once
export const NOTE = "never set rejectUnauthorized to false again";
