import { useState } from 'react';

export default function Editor() {
  const [saved, setSaved] = useState(false);
  const save = async () => {
    await fetch('/api/profile', { method: 'POST' });
    setSaved(true);
  };
  return <><button onClick={save}>Save</button>{saved && <p>Saved</p>}</>;
}
