from fastapi import APIRouter, Depends, Request
import os
import shutil
from pathlib import Path

router = APIRouter()
UPLOAD_DIR = "/srv/uploads"


@router.post("/fetch")
async def fetch(request: Request):
    target = request.query_params["file"]
    return open(os.path.join(UPLOAD_DIR, target)).read()
