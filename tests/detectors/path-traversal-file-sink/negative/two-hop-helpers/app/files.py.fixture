from fastapi import APIRouter
import os

router = APIRouter()
UPLOAD_DIR = "/srv/uploads"


def outer(name):
    return inner(name)


def inner(name):
    return os.path.join(UPLOAD_DIR, name)


@router.get("/get-file/{name}")
async def get_file(name: str):
    return open(outer(name)).read()