from fastapi import APIRouter, Depends, Request
import os
import shutil
from pathlib import Path

router = APIRouter()
UPLOAD_DIR = "/srv/uploads"


@router.get("/summary")
async def summary(name: str):
    return open(os.path.join(UPLOAD_DIR, "summary.csv")).read()
